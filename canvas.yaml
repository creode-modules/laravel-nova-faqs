preset: package

namespace: Creode\LaravelNovaFaqs
user-auth-model: App\Models\User

paths:
  src: src
  resource: resources

factory:
  path: database/factories

migration:
  path: database/migrations
  prefix: ''

console:
  namespace: Creode\LaravelNovaFaqs\Console

model:
  namespace: Creode\LaravelNovaFaqs\Entities
  src: src/Entities

provider:
  namespace: Creode\LaravelNovaFaqs\Providers

testing:
  namespace: Creode\LaravelNovaFaqs\Tests
